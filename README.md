# Task---Manager
A full-stack task management web application built with FastAPI and SQLite. It Supports complete CRUD functionality — adding, editing, marking complete, and deleting tasks. It Features a styled vanilla JavaScript frontend that communicates with the backend API, along with input validation for clean data handling.
